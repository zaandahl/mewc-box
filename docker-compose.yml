services:
  app:
    image: zaandahl/mewc-box
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        MEWC_DETECT_BASE: ${MEWC_DETECT_BASE:?Set the fixed parent image from the generated image lock}
